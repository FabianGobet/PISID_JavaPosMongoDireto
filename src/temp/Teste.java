package temp;

import main.Mainthread;

public class Teste {

    public static void main(String[] args) {
        Mainthread a = new Mainthread();
        a.connectDbs();
        /*List<Integer> OS = new ArrayList<Integer>();
        OS.add(1);
        OS.add(2);
        OS.add(3);
        OS.add(4);
        OS.add(5);
        OS.add(6);
        OS.add(7);

        double Q1;
        double Q3;

        if (OS.size() % 2 != 0)
            OS.add(OS.size() / 2 + 1, OS.get(OS.size() / 2));
        if (OS.size() / 2 % 2 != 0) {
            Q1 = OS.get((OS.size() / 2) / 2);
            Q3 = OS.get(3 * ((OS.size() / 2) / 2) + 1);
        } else {
            Q1 = (OS.get((OS.size() / 2) / 2 - 1) + OS.get((OS.size() / 2) / 2)) / 2d;
            Q3 = (OS.get(3 * ((OS.size() / 2) / 2) - 1)
                    + OS.get(3 * ((OS.size() / 2) / 2))) / 2d;
        }

        System.out.println("Q1: " + Q1 + "; Q3: " + Q3);
    */
        

    }
}
